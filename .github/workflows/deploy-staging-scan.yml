name: Deploy to Staging and Security Scan

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main]

jobs:
  # Deploy to staging environment
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master
      
      - name: Deploy to Staging
        run: flyctl deploy --config fly-staging.toml --remote-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
      
      - name: Wait for deployment to stabilize
        run: sleep 60

  # Run OWASP ZAP scan against staging
  zap-scan:
    name: OWASP ZAP Security Scan
    needs: deploy-staging
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.15.0
        with:
          target: 'https://rag-engine-staging.fly.dev'
          rules_file_name: '.zap/rules.tsv'
          cmd_options: '-a'
          fail_action: false
          allow_issue_writing: true
      
      - name: Upload ZAP Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-scan-report
          path: |
            report_html.html
            report_md.md
            report_json.json

  # Optional: Deploy to production only on main branch and if all checks pass
#   deploy-production:
#     name: Deploy to Production
#     needs: [deploy-staging, zap-scan]
#     runs-on: ubuntu-latest
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
      
#       - name: Setup Flyctl
#         uses: superfly/flyctl-actions/setup-flyctl@master
      
#       - name: Deploy to Production
#         run: flyctl deploy --config fly.toml --remote-only
#         env:
#           FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
